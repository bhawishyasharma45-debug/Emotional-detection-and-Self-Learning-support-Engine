# ── Web framework (install first) ──
streamlit>=1.35.0

# ── AI / LLM ──
google-genai>=2.0.0

# ── Core data (flexible pins — avoids source compilation on Render) ──
pandas>=2.0.0
numpy>=1.24.0
python-dotenv>=1.0.0

# ── Visualization ──
plotly>=5.18.0

# ── ML / NLP ──
scikit-learn>=1.3.0
nltk>=3.8.0

# ── Deep learning ──
# NOTE: tensorflow, torch, transformers are not compatible with Python 3.14.
# The app uses keyword-based + numpy prediction at runtime, so these are not required.
# tensorflow>=2.15.0
# torch>=2.1.0
# transformers>=4.35.0

# ── Misc ──
scipy>=1.11.0
matplotlib>=3.8.0
seaborn>=0.13.0
protobuf>=4.24.0